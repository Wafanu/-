#include <iostream>
с использованием пространства имен std;

float max(float x, float y); //объявление функции

int main() //главная функция
{
  плавающий a=5,5, b=3,2, c = 14,1, d;
  d=макс(макс(а,б),в); //1
  cout << "макс = " <<d<<endl;
  вернуть 0;
}

float max(float x, float y) // определение функции
{
  вернуть (x>y) ? x: y;
}
